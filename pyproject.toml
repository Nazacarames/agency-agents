# pyproject.toml — config de ruff (linter)
[tool.ruff]
line-length = 110
target-version = "py312"

[tool.ruff.lint]
select = ["E", "F", "W", "I", "B", "UP"]
ignore = ["E501"]  # line-length ya lo maneja ruff

[tool.ruff.lint.per-file-ignores]
"scripts/*" = ["E402"]  # path hacks permitidos

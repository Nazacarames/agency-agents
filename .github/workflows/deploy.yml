name: Deploy to Render

on:
  push:
    branches: [main]
  workflow_dispatch:

jobs:
  deploy:
    runs-on: ubuntu-latest
    steps:
      - name: Trigger Render Deploy Hook
        env:
          RENDER_DEPLOY_HOOK: ${{ secrets.RENDER_DEPLOY_HOOK }}
        run: |
          if [ -n "$RENDER_DEPLOY_HOOK" ]; then
            echo "Triggering Render deploy hook..."
            curl -fsS -X POST "$RENDER_DEPLOY_HOOK" \
              -H "Accept: application/json" \
              -w "\nHTTP %{http_code}\n" || echo "Deploy hook failed (non-fatal)"
          else
            echo "RENDER_DEPLOY_HOOK secret not configured; relying on Render autoDeploy."
          fi

      - name: Wait and smoke test
        env:
          RENDER_SERVICE_URL: https://automiq-agents.onrender.com
        run: |
          echo "Waiting 60s for Render to rebuild..."
          sleep 60
          echo "Smoke test: GET $RENDER_SERVICE_URL/healthz"
          curl -fsS "$RENDER_SERVICE_URL/healthz" | head -200 || echo "Smoke test failed (non-fatal, deploy may still be in progress)"
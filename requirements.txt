# Automiq Agency Agents — Render service
# Stack: FastAPI + APScheduler + httpx + discord-webhook (no bot)
# Importante: hermes-agent 0.15.2 pinea versiones exactas de sus deps
# (openai==2.24.0, fire==0.7.1, rich==14.3.3, pyyaml==6.0.3, tenacity==9.1.4, etc).
# Usamos >= para todas las nuestras y dejamos que pip resuelva.

# Web framework
fastapi>=0.115.0
uvicorn[standard]>=0.30.0
pydantic>=2.5.0
pydantic-settings>=2.0.0

# HTTP client para MiniMax-M3 y Discord
httpx>=0.27.0
tenacity>=9.0.0        # hermes-agent requiere 9.1.4

# Scheduler (reemplazo del cron de OpenClaw)
apscheduler>=3.10.0
pytz>=2024.1

# Discord delivery (webhook, no bot)
discord-webhook>=1.3.0

# Utilidades
python-dateutil>=2.9.0
python-dotenv>=1.0.0,<2.0
pyyaml>=6.0.0           # hermes-agent requiere 6.0.3
orjson>=3.10.0
beautifulsoup4>=4.12.0

# Render/Railway healthcheck
structlog>=24.1.0

# Browser tooling for prospecting (Playwright)
playwright>=1.40.0

# Hermes Agent runtime (paquete que provee ACP adapter, gateway, CLI)
hermes-agent==0.15.2

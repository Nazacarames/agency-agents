# Python
__pycache__/
*.py[cod]
*.egg-info/
.venv/
venv/
.env
.env.local
.env.production

# Logs
logs/*.log
logs/*.jsonl
*.log

# Data runtime (artefactos generados en ejecución — no commitear)
data/leadhunter-*.md
data/leadhunter-*.json
data/outbound-report-*.md
data/inbox-assistant-report-*.md
data/deploy-*.json
data/render-*.json
data/agent-output-*.json
data/runs/
data/.hermes/
data/*.txt
data/samples/
data/prompts/

# Secretos OAuth de Google (Gmail / YouTube) — NUNCA commitear
scripts/client_secret.json
scripts/gmail_token.json
scripts/youtube_token.json
**/client_secret*.json
**/gmail_token*.json
**/youtube_token*.json

# Tokens y estado OAuth de TikTok — NUNCA commitear
data/tiktok-token.json
data/tiktok-oauth-state.json

# Token y estado OAuth de LinkedIn — NUNCA commitear
data/linkedin-token.json
data/linkedin-oauth-state.json

# Webhooks de Discord (URLs = secreto del canal) — NUNCA commitear
scripts/_discord_*.json

# Sent-log de outbound (emails de leads contactados) — runtime del volume
data/outbound-sent.json

# Leads store (CRM/pipeline con datos de contacto) — runtime del volume, NUNCA commitear
data/leads-store.json
data/leads-store.json.tmp

# Stores de memoria/agencia + imágenes generadas — runtime del volume, NUNCA commitear
data/clients-store.json
data/client-memory.json
data/agency-memory.json
data/meetings-store.json
data/missions-store.json
data/tasks-store.json
data/finance-store.json
data/fx-store.json
data/metrics-store.json
data/publish-queue.json
data/ads-store.json
data/inbox-processed.json
data/hook-vault.json
data/competitor-radar.json
data/trend-radar.json
data/creative-direction.md
data/*.json.tmp
data/images/

# Scratch local (logs bajados de Railway, etc.)
_lg.json

# Editor
.vscode/
.idea/
.DS_Store

# Render
.render/

# OS
Thumbs.db

# visual scout scratch (sheets/captions descargados; se regeneran)
data/scout/
